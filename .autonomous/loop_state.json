{
 "_doc": "Crash/resume file for the 15-min paid check-in loop (Het, 2026-08-26: 'use it every 15 mins... make sure if you are not left tokens you have a crash file to store what you did before the limit so you can start working again as session resets'). Every firing reads this FIRST. status='idle' means the previous firing finished cleanly or found nothing to do -- start fresh. status='in_progress' means a previous firing got cut off mid-task -- resume from resume_instructions EXACTLY, don't restart from scratch and don't second-guess what's already been committed. Update and push this file at the START of any multi-step task (before the risky part), updated again on finish or safe stop.",
 "status": "in_progress",
 "task": "ACCELERATION PLAN: benchmark backfill tool + historical strategy validation tool + retire-dead-strategies evidence pass",
 "started_at": "2026-09-13T10:30:00Z",
 "last_updated": "2026-09-13T10:30:00Z",
 "resume_instructions": "Het authorized THREE things in-session 2026-09-13 ('Yes start working on it'), after being shown real numbers: (1) BACKFILL nifty_benchmark's history from public index data -- it is buy-and-hold, makes no decisions, so its past returns are public fact not reconstructed discretion; this removes the ~6-week promotion-clock block from the Q4 finding. (2) MERGE the diagnostic tooling to main -- REQUIRED because GitHub's workflow_dispatch will not run a brand-new workflow file that only exists on a branch, and this sandbox cannot reach ANY market data source (yfinance blocked, FRED blocked -- both verified). (3) RETIRE strategies that never worked, based on a historical validation backtest -- 15 of 25 live contestants have never traded, and they raise the multiplicity bar for everyone else (measured: K=25 -> K=10 lowers the Sharpe floor 10.5% and cuts days-to-certify ~20%). PLAN: build tools/backfill_benchmark_history.py (dry-run default, idempotent, touches ONLY nifty_benchmark) + tools/validate_strategies_historical.py (read-only, runs each live registry entry's REAL params through its sig_* over 5y history and reports whether it would EVER have cleared the real promotion_check bar) + workflows for both; test locally with synthetic data; write .autonomous/ACCELERATION_PLAN.md as the detailed low-token work list; commit+push to branch; THEN merge tooling to main (authorized); THEN dispatch. RETIREMENT ITSELF IS NOT AUTO-APPLIED -- produce the evidence, show Het, retire only after he sees the list. If resuming: check git log for which of these files already exist, finish the missing ones. NEVER touch RULES/LADDER/COST_PER_SIDE. NEVER backfill any contestant other than nifty_benchmark."
}
