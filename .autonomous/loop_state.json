{
 "_doc": "Crash/resume file for the 15-min paid check-in loop (Het, 2026-08-26: 'use it every 15 mins... make sure if you are not left tokens you have a crash file to store what you did before the limit so you can start working again as session resets'). Every firing reads this FIRST. status='idle' means the previous firing finished cleanly or found nothing to do -- start fresh. status='in_progress' means a previous firing got cut off mid-task -- resume from resume_instructions EXACTLY, don't restart from scratch and don't second-guess what's already been committed. Update and push this file at the START of any multi-step task (before doing the risky part) and again when finishing or safely stopping -- see next_session.md and the Routine's own prompt for the full protocol.",
 "status": "in_progress",
 "task": "Q5 promotion-bar fix + Q6 breeding gate (Het authorized both, 2026-08-30)",
 "started_at": "2026-08-30T07:46:46Z",
 "last_updated": "2026-08-30T07:46:46Z",
 "resume_instructions": "Het gave FRESH EXPLICIT in-session authorization on 2026-08-30 for BOTH: (Q5) promotion bar = require beating nifty_benchmark AND a multiplicity correction for the number of live contestants; (Q6) add days_on_rung >= RULES['min_days_on_rung'] to attempt_breeding()'s eligibility check, mirroring propose_evolutions(). THIS AUTHORIZES THE CODE CHANGE ON THE BRANCH ONLY -- merging to main still needs its own separate fresh confirmation, never assume it. CRITICAL ORDERING (MASTER_PLAN.md acceptance criteria): implement and commit the bar change BEFORE looking at which current contestants it would promote or fail -- that ordering is what keeps it free of hindsight bias. Do NOT run Q4 (does anything beat Nifty) until after. Test standard: 40-cycle synthetic regression + report() in an isolated scratch dir (RUNBOOK 4 Step 4.3), plus re-run tools/analyze_statistical_power.py against the NEW gate and show the false-positive rate under ~10%. If interrupted mid-way: check git log on the branch for what already landed before redoing anything.",
 "note": "Q5+Q6 implementation in progress, authorized by Het 2026-08-30."
}